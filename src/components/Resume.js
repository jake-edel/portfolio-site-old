import resume from '../je_resume.png'

const Resume = () => {
    return(
        <div className='resume-container'>
            <img src={resume} className='resume' alt='Jakob Edelstein Resume'/>
        </div>
    )
}

export default Resume